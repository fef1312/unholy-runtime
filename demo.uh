
let someVariable: int;

/*
 * multiline comment
 */

/**/// There are two different kinds of comments in this line
// Single-line comment


func main(): int {
    let var2: int;
    let someBool: bool = true;
    if (2 > 3) {
        var2 = 3;
    } else {
        anotherFunc(500, true);
    }
}

func anotherFunc(someParam: int, anotherParam: bool): int {
    let varInsideAnotherFunc: int;
    return 1 + 2 * 3 - 4;
}
